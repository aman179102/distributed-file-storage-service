apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: filestore
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "filestore"
            - name: POSTGRES_USER
              value: "filestore"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: filestore-secrets
                  key: DB_PASSWORD
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "filestore"]
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 50Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: filestore
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
